vector<int> result = solution.findDuplicates(nums); // Find duplicates
    
    // Print duplicates
    for (int num : result) {
        cout << num << " ";
    }
    cout << endl;
    
    return 0;